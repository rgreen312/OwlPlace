import React, { FC } from "react";
import "./App.scss";
import RoutingContainer from './RoutingContainer';

const App: FC = () => {
  return (
    <div className='App'>
      <RoutingContainer />
    </div>
  );
};

export default App;
