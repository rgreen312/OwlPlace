import React from "react";
import "./Header.scss";

const Header: React.FC = () => {
  return (
    <div>
      <div className="header">Owl Place</div>
      <div className="sub-header">change the canvas one pixel at a time</div>
    </div>
  );
};

export default Header;
